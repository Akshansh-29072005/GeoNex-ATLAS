# Internal Directory

This directory (`internal/`) contains private application code.

## Subdirectories (Planned)
- `middleware/`: Auth and Logging middleware
- `config/`: Configuration loading (Env vars)
- `database/`: DB connection pooling (PostgreSQL)
